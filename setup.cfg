[flake8]
max-line-length = 100
exclude = .git,__pycache__,.venv,venv,.terraform,build,dist
# E203/W503 conflict with common formatting; ignore for compatibility.
extend-ignore = E203,W503

[tool:pytest]
testpaths = tests
addopts = -v --cov=src --cov-report=term-missing --cov-report=xml
